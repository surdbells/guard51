import { Component } from '@angular/core';

@Component({
  selector: 'g51-tenants',
  standalone: true,
  template: `<div>
    <h1 class="text-2xl font-semibold" style="color: var(--text-primary)">Tenant Management — Batch 6</h1>
  </div>`,
})
export class TenantsComponent {}
