import { Component } from '@angular/core';

@Component({
  selector: 'g51-forgot-password',
  standalone: true,
  template: `<div class="flex items-center justify-center min-h-screen" style="background: var(--surface-bg)">
    <p style="color: var(--text-secondary)">Forgot password — Batch 5</p>
  </div>`,
})
export class ForgotPasswordComponent {}
