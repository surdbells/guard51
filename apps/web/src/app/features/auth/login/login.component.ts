import { Component } from '@angular/core';

@Component({
  selector: 'g51-login',
  standalone: true,
  template: `<div class="flex items-center justify-center min-h-screen" style="background: var(--surface-bg)">
    <p style="color: var(--text-secondary)">Login page — Batch 5</p>
  </div>`,
})
export class LoginComponent {}
